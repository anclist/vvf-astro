## Purpose
Provides design tokens for consistent styling across components.

## ADDED Requirements

### Requirement: Color palette
The system SHALL provide the tokens: primary, secondary, background, surface, error.

#### Scenario: Token usage
- **WHEN** a component uses the `primary` token
- **THEN** the color is applied consistently.
