---
export interface Props {
  href?: string
  variant?: 'primary-sky' | 'outline-sky' | 'primary-navy' | 'outline-navy' | 'icon'
  size?: 'sm' | 'md' | 'lg'
  class?: string
  [key: string]: any
}

const { href, variant = 'primary-sky', size = 'md', class: className = '', type, ...rest } = Astro.props as Props

const sizes = {
  sm: 'px-4 py-2 text-sm',
  md: 'px-5 py-2',
  lg: 'px-6 py-2',
}

const variants = {
  'primary-sky': 'bg-brand-sky text-white hover:bg-brand-sky-dark',
  'outline-sky': 'border border-brand-sky text-brand-sky hover:bg-brand-sky/10',
  'primary-navy': 'bg-brand-navy text-white',
  'outline-navy': 'border border-brand-navy/30 text-brand-navy hover:bg-white/40',
  icon: '',
}

const base =
  variant === 'icon'
    ? 'shrink-0 rounded-full border p-2'
    : `rounded-pill font-medium ${sizes[size]} ${variants[variant]} disabled:opacity-50 disabled:cursor-not-allowed`

const Tag = href ? 'a' : 'button'
---

<Tag href={href} type={!href ? (type ?? 'button') : undefined} class={`${base} ${className}`} {...rest}>
  <slot />
</Tag>
