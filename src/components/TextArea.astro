---
export interface Props {
  class?: string
  [key: string]: any
}

const { class: className = '', ...rest } = Astro.props as Props
---

<textarea class={`rounded-2xl px-4 py-3 border ${className}`} {...rest}></textarea>
